Abdulhamid Lawal  Shuaibu
230504523
REPORT DAY 1


What i did today

Installed Node.js, set up Expo, installed Expo Go on my phone
Created the "Salah" project with create-expo-app
Ran it live on my phone (Hello World with hot reload)
Built a static prayer-times screen using View, Text, ScrollView
Hardcoded the 5 prayers in styled cards, softened the colors to a light palette
Set up Git, created a public GitHub repo, committed and pushed all my work which claude helped me a bit


What I learned

I first used Node.js = runtime for JS tooling. 

Then I downloaded Expo Go on my phone and used QR codes, which simplifies React Native; Expo Go actually runs the app on phone without compiling
Apparently, Hot reload  saving updates the phone instantly
View is kind of a container, while Text holds all text and ScrollView makes the content scrollable.
flexDirection: 'row' + justify Content: 'space-between' = side-by-side layout
Basic Git workflow: add to commit to push
Problems & how I solved them
Streak folder didn't show in VS Code; confirmed it existed via dir, opened with code .
 Font size typo (should be fontSize) + missing commas caused syntax errors and i also ran into problems likee fixed capitalization errors and commas
Git needed my name/email before committing; set them with git config
Terminal echoed git output as fake errors, then confirmed success with git status

Day 2: Tomorrow
I will look into styling.( Flexbox ) 
